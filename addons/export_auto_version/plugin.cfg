[plugin]

name="Export Auto Version"
description="Stamps every export with a base version from Project Settings plus a git tag: the commit count for a numeric build, or the short commit sha. Writes application/config/version, so anything already reading it gets the release for free."
author="Mark Arneman"
version="1.0.0"
script="export_auto_version_plugin.gd"
